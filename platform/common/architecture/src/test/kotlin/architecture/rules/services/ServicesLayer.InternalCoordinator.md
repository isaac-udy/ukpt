# Coordinators (`services.internal`)

* **Definition**: The orchestrators that compose subsystems (e.g. `SessionProcessingManager`) — see the [`services.internal` overview](#servicesinternal). Cross-subsystem composition belongs here, at bare `services.internal`, not to imports between sibling subsystems.
