# Data carriers (`services.internal`)

* **Definition**: Payloads that flow from one subsystem through the orchestrator into another. A carrier lives at the bare `services.internal` ancestor so both producer and consumer can name it under the data-shape carve-out (see [hierarchical visibility](#hierarchical-visibility-within-servicesinternal)).
